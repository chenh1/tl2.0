const navSpacing = 48;
const logoMaxWidth = 60;

export default {
  logoMaxWidth: `${logoMaxWidth}px`,
  baseLogoMaxWidth: '40px',
  base: `${logoMaxWidth / 2}px`,
  lg: `${navSpacing + (logoMaxWidth / 2)}px`,
};